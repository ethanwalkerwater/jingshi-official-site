# 待补充信息清单

第一版已用真实师资 + 占位大图把网站填满。以下是上线前需要你补充的内容，按优先级排列。

## 高优先（影响可信度与转化）

- [ ] **联系方式**：`src/data/site.ts` 里 `contact.phone / wechat / email` 现为占位，替换为真实信息。
- [ ] **微信二维码**：把官方微信二维码图存为 `public/images/wechat-qr.png`，可在预约弹窗与页脚展示。
- [ ] **预约表单对接**：当前提交仅前端反馈（见 `src/components/booking.tsx` 的 `handleSubmit`，标了 TODO）。
      需对接真实渠道——邮件、表单服务（如 Formspree）、或企业微信/飞书机器人。
- [ ] **另外 2 个校区地址**：`site.ts` 的 `campuses` 里「滨江校区」「徐滨校区」待填（现写"地址待补充"）。
- [ ] **老师三项评分**：`src/data/teachers.ts` 每位老师的 `ratings`（学习提升效果 / 责任心与服务态度 / 个人魅力，1–5 分）
      ⚠️ 目前为占位分（4.6–5.0），请替换为真实评测分数。卡片角标"综合评分"为三项平均，自动计算。

## 中优先（提升品牌质感）

- [ ] **真实 logo 文件**：网站已改为海军蓝 + 金配色，与 logo 一致；但顶栏/页脚目前用文字标识（KA 字标）。
      请提供透明背景的横版/竖版 logo（深色底反白一份、浅色底一份），放进 `public/`，再在 `Header.tsx` / `Footer.tsx` 换成图片。
- [ ] **品牌实拍图**：替换 Hero、理念、课程、预约区、名师页头图的 Unsplash 占位图
      （校区环境、上课场景、师生合影等），统一在 `site.ts` 的 `images` 里改。
- [ ] **成绩与案例**：offer 捷报、提分数据、名校录取名单、家长口碑。
      内容齐了可新增一个「成果」板块或独立页面。
- [ ] **关键数据核实**：Hero 的 `stats`（20+/5/10+/一站式）按真实数字校准。

## 低优先（合规与扩展）

- [ ] **ICP 备案号**：页脚现为占位「沪ICP备 XXXXXXXX 号」。
- [ ] **公司主体全称**：`site.ts` 的 `copyright`。
- [ ] **后续可加页面**（内容齐备后）：独立课程详情页、关于我们页、校区页、成果案例页。

## 内容核对

- [ ] 课程清单里原始数据中的 **「KA / КA」** 已按品牌缩写处理、未列为课程，确认无误。
- [ ] 老师简介、标签均来自教研「老师卡片」，如有更新请同步 `teachers.ts`。
